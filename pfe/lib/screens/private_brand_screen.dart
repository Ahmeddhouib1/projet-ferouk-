import 'package:flutter/material.dart';

class PrivateBrandScreen extends StatelessWidget {
  const PrivateBrandScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Découvrez nos marques privées", style: TextStyle(fontSize: 18)),
    );
  }
}
